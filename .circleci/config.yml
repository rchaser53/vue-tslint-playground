version: 2
jobs:
  build:
    docker:
      - image: circleci/node:6.10.3

    steps:
      - restore_cache:
          keys:
          - dependencies-{{ checksum "package.json" }}

      - run: yarn install

      - save_cache:
          paths:
            - node_modules
          key: dependencies-{{ checksum "package.json" }}
        
      - run: 
          name: test
          command: echo 23ß